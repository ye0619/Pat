<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 加速度计/陀螺仪：Android 系统传感器，无需运行时权限 -->
    <!-- VIBRATE：Normal 权限，安装时自动授予，API 1+ -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- POST_NOTIFICATIONS：运行时权限（Android 13+ / API 33+），需动态请求 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- FOREGROUND_SERVICE：API 29+ 前台服务必需（API 29 以下自动授予） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- FOREGROUND_SERVICE_SPECIAL_USE：API 34+ 使用 specialUse 类型时必需 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pat">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:label="@string/app_name"
            android:theme="@style/Theme.Pat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 前台服务：承载 ScreenMonitor 和 BatteryMonitor，确保屏幕事件不依赖 Activity 生命周期 -->
        <service
            android:name=".service.MonitorForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="device_state_monitoring_for_context_aware_companion" />
        </service>
    </application>

</manifest>